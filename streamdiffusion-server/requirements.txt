# StreamDiffusion pins diffusers==0.24.0 — do not override.
# Install StreamDiffusion from git first, then these server-only deps.
# pip install git+https://github.com/cumulo-autumn/StreamDiffusion.git@main#egg=streamdiffusion

# Server
fastapi>=0.108.0
uvicorn[standard]>=0.25.0
websockets>=12.0

# Image processing
Pillow>=10.0.0
