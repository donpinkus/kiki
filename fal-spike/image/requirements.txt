# Runner dependencies for the fal image-spike app.
#
# These mirror the FLUX/image portion of model-servers/requirements.txt and are
# the SOURCE OF TRUTH for the `requirements = [...]` list in app.py — keep the two
# in sync by hand (isolation rule: no shared imports between fal-spike and
# model-servers). torch/torchvision are provided by fal's base image; do NOT pin
# them here (same reasoning as the RunPod server — pinning would override the
# base image's Blackwell-compatible build).
#
# This file is NOT auto-consumed by fal (fal reads the class `requirements`
# attribute). It exists for documentation + local lint. The LTX/video deps are
# intentionally omitted — the spike is image-only.

diffusers @ git+https://github.com/huggingface/diffusers.git
transformers>=4.52,<5
accelerate>=0.28.0,<2
sentencepiece>=0.2.0
safetensors>=0.4.0
huggingface-hub>=0.25.0
Pillow>=10.0.0
